import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export const dynamic = "force-dynamic";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      label="Compte"
      title="Paramètres"
      description="Configuration de votre auto-école."
    />
  );
}
