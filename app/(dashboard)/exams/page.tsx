import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export const dynamic = "force-dynamic";

export default function ExamsPage() {
  return (
    <PagePlaceholder
      label="Gestion"
      title="Examens"
      description="Gestion des examens code et conduite."
    />
  );
}
